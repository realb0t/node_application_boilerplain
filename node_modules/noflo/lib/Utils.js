// Generated by CoffeeScript 1.3.1
(function() {
  var noflo;

  noflo = require('./NoFlo');

  exports.saveJSON = function(graph, filename) {};

  exports.loadJSON = function(filename, success) {
    var graph, network;
    graph = new noflo.Graph('Load JSON files into a NoFlo Graph');
    graph.addNode('Read', 'ReadFile');
    graph.addNode('Parse', 'ParseJson');
    graph.addNode('Display', 'Output');
    graph.addNode('Success', 'Callback');
    graph.addEdge('Read', 'out', 'Parse', 'in');
    graph.addEdge('Read', 'error', 'Display', 'in');
    graph.addEdge('Parse', 'out', 'Success', 'in');
    graph.addInitial(filename, 'Read', 'source');
    graph.addInitial(function(data) {
      return console.log("CALLED", data);
    }, 'Success', 'callback');
    return network = noflo.createNetwork(graph, true);
  };

  exports.loadFBP = function(filename, success) {};

  exports.loadJSON('examples/linecount/count.json');

}).call(this);
